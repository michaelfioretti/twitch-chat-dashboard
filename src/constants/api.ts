const API_BASE_URL = 'https://twitch-chat-api.onrender.com/api';

export const API_ENDPOINTS = {
  OVERVIEW: `${API_BASE_URL}/stats`,
  BITS: `${API_BASE_URL}/bits`,
  TOP_STREAMERS: `${API_BASE_URL}/streamer/top10`,
}
